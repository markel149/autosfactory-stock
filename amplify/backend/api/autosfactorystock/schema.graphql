type Coche @model @auth(rules: [{allow: public}]) {
  id: ID!
  marca: String
  modelo: String
  matricula: String
  color: String
  kilometros: Int
  precioCompra: Float
  precioVenta: Float
  notas: String
}
 